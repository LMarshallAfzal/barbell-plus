import 'package:flutter/material.dart';

class FunctionList extends StatelessWidget {
  const FunctionList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      width: double.infinity,
      height: 276,
      child: const Text('Function List'),
    );
  }
}
